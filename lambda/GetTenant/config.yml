GetTenant:
  handler: ./lambda/GetTenant/index.main
  description: "Get a tenant using the Id"
  memorySize: 512
  timeout: 10 # API Gateway has a timeout of 30 seconds
  environment:
  iamRoleStatements:
    - Effect: Allow
      Action:
        - dynamodb:GetItem
      Resource:
        - ${self:custom.PropertyServiceTable.arn}
  events:
    - http:
        method: GET
        path: /tenants/{id}/{email}